# Laravel API base URL (no trailing slash). Used for protocols, threads, comments, reviews, votes.
# Example: http://localhost:8000 or https://your-laravel-api.com
NEXT_PUBLIC_API_URL=http://localhost:8000
